\documentclass{article}

\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\begin{document}
\pagestyle{empty}


% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=blue!10, 
    text width=4.5em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!10, 
    text width=5em, text badly centered, rounded corners, minimum height=7em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [draw, ellipse,fill=red!4, node distance=2cm, minimum height=3em, text badly centered]
\tikzstyle{cloud1} = [draw, ellipse,fill=green!4, node distance=2cm, minimum height=4em, text width=6em , text badly centered]
    
\begin{tikzpicture}[node distance = 2cm, auto]
    % Place nodes
    %\node [cloud] (icon) {ICON model};
    \node [block] (init) {Scene generator};
    \node [block, right of=init, node distance = 3cm] (arts) {ARTS};
    \node [cloud, above of=init, node distance = 3cm ] (harmonieinput) {HARMONIE-AROME};
    \node [cloud, below of=arts, node distance = 3cm] (psd) {PSD};
    \node [cloud1, below of=init, node distance = 3cm] (sie) {Surface emissivity};
    \node [cloud1, left of= sie, node distance = 5cm] (sic) {Sea-ice concentration};
    \node [block, left of= init, node distance = 3cm] (classify) {Sea-ice classification};
	\node [cloud, right of= arts, node distance = 2cm](tb) {$T_b$};
	\node [cloud, left of = classify, node distance = 3cm] (sar) {SAR};
	\node [block, right of=tb, node distance = 2cm] (qrnn) {QRNN};
	\node [cloud1, above of=qrnn, node distance = 3cm] (wvp) {WVP};
	\node [cloud1, below of=qrnn, node distance = 3cm] (lwc) {Liquid Water Content};
	
    %\node [block, left of=evaluate, node distance=3cm] (update) {update model};
    %\node [decision, below of=evaluate] (decide) {is best candidate better?};
    %\node [block, below of=decide, node distance=3cm] (stop) {stop};
    % Draw edges
    %\path [line] (icon) -- (init);
    \path [line] (harmonieinput) -- (init);
    \path [line] (psd) -- (arts);
    \path [line] (sie) -- (arts);
    \path [line] (arts) -- (tb);
    \path [line] (init) -- (arts);
    \path [line] (sar) -- (classify);
    \path [line] (tb) -- (qrnn);
    \path [line] (qrnn) -- (wvp);
    \path [line] (sie) -- (sic);
    \path [line] (classify) -- (sie);
    \path [line] (classify) -- (init);
    \path [line] (qrnn) -- (lwc);
    %\path [line] (update) |- (identify);
    %\path [line] (decide) -- node {no}(stop);
    %\path [line,dashed] (expert) -- (init);
    %\path [line,dashed] (system) -- (init);
    %\path [line,dashed] (system) |- (evaluate);
\end{tikzpicture}


\end{document}